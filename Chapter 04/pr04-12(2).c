#include <stdio.h>

int main(void) {
    int i = 255;

    i = -1;
    printf("-1을 16진수로 출력: %x\n", i);

    i = -2;
    printf("-2를 16진수로 출력: %x\n", i);

    i = -3;
    printf("-3을 16진수로 출력: %x\n", i);

    return 0;
}